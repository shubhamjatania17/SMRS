# SMRS

A smart medical response system for college campuses.

## Run

1. Start the backend:

```bash
npm start
```

1. Open the app at `http://localhost:3000`.

## API

The backend exposes demo endpoints for login, active alerts, alert creation, and resolution under `/api/*`.
